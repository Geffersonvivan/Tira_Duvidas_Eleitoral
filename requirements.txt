# Dependências de runtime para o deploy (Railway/Nixpacks).
# Mantém paridade com [project].dependencies do pyproject.toml.
Django>=5.1,<5.2
djangorestframework>=3.15
anthropic>=0.40
httpx>=0.27
psycopg[binary]>=3.2
pgvector>=0.3
pillow>=11.0
pypdf>=5.0
python-docx>=1.1
python-dotenv>=1.0
gunicorn>=23.0
whitenoise>=6.7
# Observabilidade — ativo só quando SENTRY_DSN está definido (config/settings.py).
sentry-sdk>=2.0

# --- Auth (Clerk) — mesmos pisos de segurança do PJARI (pip-audit) ---
# PyJWT valida o JWT do Clerk em core/middleware.py; svix verifica a
# assinatura dos webhooks (obrigatória, fail-closed).
PyJWT>=2.13.0
cryptography>=48.0.1
svix>=1.0
requests>=2.32

# --- RAG · ingestão do Google Drive (fonte viva) ---
google-api-python-client>=2.130
google-auth>=2.30
# OCR de PDFs escaneados (o OCR roda onde houver tesseract+poppler no SO;
# no Railway a ingestão lê o texto [OCR] já gravado no Drive, sem re-OCR).
pytesseract>=0.3.13
pdf2image>=1.17
